sl_status_t tempfunc(int16_t *temp_BLE);//il s'agit d'un prototype(c'est son nom) dans un header on appelle le nom des fonction du .c.
                                       //C'est comme si le .h est un menu et .c est la recette.
